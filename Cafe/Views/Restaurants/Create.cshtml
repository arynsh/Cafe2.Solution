@{
  Layout = "_Layout";
}

@model Cafe.Models.Restaurant

<h4>Add a new restaurant</h4>

@using (Html.BeginForm())
{
   
    @Html.LabelFor(model => model.Name)  // Display's the property name
    @Html.TextBoxFor(model => model.Name) // Creates input field 

    @Html.LabelFor(model => model.Cuisine)
    @Html.DropDownList("CuisineId")

    <input type="submit" value="Add new item" class="btn btn-default" />
}
<p>@Html.ActionLink("Show all restaurants", "Index")</p>
`